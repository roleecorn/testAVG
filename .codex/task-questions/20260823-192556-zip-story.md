# 角色劇情 ZIP 匯入問題

- status: open
- run: `tmp/character-story-import/drive-download-20260823T111855Z-1-001/20260823-192556`
- archive_sha256: `49CE655BDA170D9FA6264AFB84C82CA518685748D800E11257F02C9E3B54CEC6`

## 非阻塞判定

## 使用者已確認的處理決策

- 使用 `茜(添加CG的修正版).txt` 作為茜本次完整來源候選。
- 新支線原 ID `416` 已依使用者指示改為可露凱，runtime slug 採用 `kelukai`；另一支線維持 `mysterious-banana`。
- ZIP 圖片按用途分流：CG／背景直接使用；角色立繪參考交由 `anime-expression-grid` 生成六表情差分。
- 非 544×416 背景允許等比裁切／置中生成 runtime 背景。

- 茜資料夾同時包含 `茜.txt` 與 `茜(添加CG的修正版).txt`。兩者完整內容只差新增兩個 `【ＣＧ：願與你同行 出現/消失】` 指令；本次暫採帶有「添加CG的修正版」完整文本作為 `project/story/茜.txt`，保留另一份原始檔與 SHA-256 於本次 run 證據。
- `9.地子(風揚)/比那名居地子篇.docx` 已抽取為 UTF-8 工作文本，正文角色名確認為「比那名居地子」；沿用既有 `dizi` ID，因為專案現有入口、IR 與 floor 已使用此 ID。
- `16.416/416.txt` 與 `3.神秘香蕉人(一蕉)/神秘香蕉人.txt` 已確認為新支線來源；原 416 支線現以 `project/story/可露凱.txt`、`kelukai` Story IR／floor 與 Akiba 首段入口追蹤，另一支線維持 `mysterious-banana`。
- `17.蘭斯` 目前只有圖片，沒有角色支線來源文本；使用者已明確要求移除 `Cg00011.png` 與 `Sill-01_(2).png`，不建立劇情或 runtime 入口。原始 ZIP／run raw 仍保留追溯證據。
- 本批已完成既有角色 CG／配角立繪接入，並建立可露凱／mysterious-banana 新角色 IR／floor；本次 ZIP 圖片已全部離開 `unknown/`。神秘香蕉人來源內列出的缺少 CG 是「來源要求但 ZIP 未提供」，不視為未知來源圖片，另列 TODO。

## DOCX 提取正規化紀錄

- 原先的提取物把段落分隔寫成字面 `\\n`，並混入 CRLF；不得再作為權威來源。
- 已新增 `scripts/extract_docx_text.py`，直接讀取本次 raw DOCX 的 `word/document.xml`，按 `w:p` 保留段落、`w:br`／`w:cr` 保留手動換行、`w:tab` 保留定位字元，輸出 UTF-8 無 BOM、LF-only TXT。
- 重新提取結果已驗證：三份 DOCX 均無 BOM、無 CR、無字面 `\\n`。
- 地子 DOCX 是其唯一完整來源，已用正規化結果覆蓋 `project/story/比那名居地子.txt`，新 SHA-256：`9a590d9ae622f01b65a36e4d2bef9f29ab586029cafd6c5eebd90e8d6fdc683f`。
- 香蕉人／茜 DOCX 與 ZIP 內 TXT 不是同一版本；依角色來源優先級保留已確認的 TXT，DOCX 僅保存為已提取比較證據，不直接覆蓋。
- ZIP 內 TXT 另有 UTF-8 BOM 與 CP950 混用；`漆原瑠華.txt`、`柏崎星奈.txt` 曾因以 UTF-8 解碼出現替換字元。已用 `scripts/normalize_story_text.py` 逐份嚴格解碼並輸出 UTF-8 無 BOM／LF-only，完成後重新同步 IR 與來源 manifest。

## 受影響範圍

若後續發現角色 ID、版本選擇或演出指令無法由完整來源與既有入口證明，僅停止該角色的來源落地、素材接入與 scene/floor 整合；其他角色可繼續。
