# Task questions — ZIP story asset refresh

- Status: open
- Scope: `drive-download-20260816T094408Z-1-001.zip`
- Evidence run: `tmp/character-story-import/drive-download-20260816T094408Z-1-001/20260816-174815/`

## Questions / unresolved work

1. The ZIP contains complete sources for `漆原瑠華` (`漆原瑠華.txt`), `IDW` (`IDW.txt`), and `柏崎星奈` (`柏崎星奈.txt`), but the repository has no corresponding Story IR, floor list, or existing event-entry owner. Their source text also contains CG / cast directives that need explicit runtime mapping. These branches are not landed until their first playable scene, entry, and unresolved visual requirements are implemented.
2. Resolved by user confirmation: the remaining 14 ZIP images are source images for the corresponding chapter protagonists' generated images. They are now classified as `generated-source` in the manifest and removed from `unknown/`; `師匠.png` is directly used by `shirou_3` as `project/images/shirou_shisho.png` for 店長「東方不敗」. New-role outputs that are not present in the current runtime inventory remain marked pending without invented paths.
3. `6.月讀愛/圖檔/便利店.jpg` and `熟悉的街角.png` are story-specific background candidates but are not 544×416. The current run will derive deterministic 544×416 runtime backgrounds by aspect-preserving center crop and record the source/output relationship; formal art replacement remains an open TODO if the crop is not accepted visually.
