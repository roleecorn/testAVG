# Task Questions

- Created: `YYYY-MM-DD HH:mm:ss +08:00`
- Task: `<task name>`
- Overall status: `open | resolved`（只有所有受影響工作完成並具備驗證證據時才能使用 `resolved`）
- Long-term TODO: `<path or none>`

## Questions

### Q1. `<short title>`

- Classification: `blocking | non-blocking`
- Status: `open | resolved`（決定已作出、規格已定稿、延後或等待都必須維持 `open`）
- Source: `<request, file, scene, or command>`
- Affected scope: `<paths or workflow stages>`
- Temporary handling: `<what may continue and what must stop>`
- Decision needed: `<specific question>`
- Decision / current direction: `<answer, decision, or pending>`
- Remaining work: `<work still required; write none only when actually complete>`
- Completion evidence: `<changed paths and validation results; pending while open>`
- Resolved at: `<timestamp; pending while open>`

## Promotion

List each open question promoted to a long-term TODO, including work that has a recorded decision but remains deferred. Write `None` only when every question is resolved with completion evidence.
