# 支線立繪重新生成：原始 ZIP 缺失

- 狀態：open / blocking
- 範圍：支線角色立繪重新生成
- 發現時間：2026-08-24 Asia/Taipei

## 問題

使用者要求重新處理支線角色，且明確要求先從 ZIP 找到每個角色的實際參考圖。工作區目前只有先前 run 的解壓結果：

`tmp/character-story-import/drive-download-20260823T111855Z-1-001/20260823-192556/raw/`

沒有找到對應的原始 ZIP，因此目前無法重新計算原始 ZIP SHA-256，也無法依專案規則建立「本次全新解壓」的證據。

## 影響

在原始 ZIP 未重新提供前，所有支線角色的正式表情生成與接入都必須暫停。舊 `raw/` 不得冒充本次輸入；角色名稱、職業、檔名或舊盤點也不得替代實際參考圖。

## 已採取措施

- 尚未把任何新生成結果寫入 `project/images/`。
- 尚未恢復或採用先前被撤回的生成結果。
- 已將角色畫風規範改為：ZIP 原圖永遠只作生成參考，正式立繪必須重新生成六種表情差分。
- 主線角色仍維持暫停，這個問題只阻塞支線角色範圍。
- 已檢查 `D:\\coding\\mota-js` root 及其子目錄（含隱藏項目）；未找到對應支線 ZIP。找到的兩個 `.zip` 分別是 FFmpeg 工具包與西部決鬥小遊戲來源包，均不是本次角色資料包。

## 解阻塞條件

重新提供原始支線 ZIP，或提供可核對的原始 ZIP 絕對路徑。收到後必須從該 ZIP 計算 SHA-256、建立新的空白 run、重新解壓至新的 `raw/`，再逐角色盤點與生成。
